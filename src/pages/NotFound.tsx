import { Container, Typography } from "@mui/material";

const NotFoundPage = () => {
  return (
    <Container>
      <Typography
        variant="h1"
        component="h1"
        mt={4}
      >Page Not Found</Typography>
    </Container>
  );
};

export default NotFoundPage;