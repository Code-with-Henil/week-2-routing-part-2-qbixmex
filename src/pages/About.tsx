import { Container, Typography } from '@mui/material';

const AboutPage = () => {
  return (
    <Container>
      <Typography
        variant="h1"
        component="h1"
        mt={4}
      >About Page</Typography>
    </Container>
  );
};

export default AboutPage;