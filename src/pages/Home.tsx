import { Container, Typography } from '@mui/material';

const HomePage = () => {
  return (
    <Container>
      <Typography
        variant="h1"
        component="h1"
        mt={4}
      >Home Page</Typography>
    </Container>
  );
};

export default HomePage;
